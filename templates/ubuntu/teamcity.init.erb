#!/bin/sh

<%= "export TEAMCITY_DATA_PATH='#{@data_path}'" if @data_path %>

case $1 in
	start)
		start-stop-daemon --start -c <%= @user %> --exec <%= @script_path %> start
	;;
	       
	stop)
		start-stop-daemon --start -c <%= @user %> --exec <%= @script_path %> stop
	;;
esac
       
exit 0
